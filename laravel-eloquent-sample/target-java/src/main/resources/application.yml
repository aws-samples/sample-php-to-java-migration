server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:mem:bookstack;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    open-in-view: false
